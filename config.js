// La Recetera — the only file you edit to change where the API calls go.
//
// TWO WAYS TO RUN THIS:
//
// 1. Published (GitHub Pages) — put your proxy URL below.
//    The Groq key lives inside the proxy, never in this repo, never in the
//    browser. See worker.js + README.md for the 5-minute setup.
//
// 2. Local only — leave proxyUrl empty. The app asks for your Groq key once
//    and keeps it in your own browser's localStorage. The key is never
//    written into these files, so nothing secret ever gets committed.
//
// Never paste a Groq key into this file.

window.RECETERA_CONFIG = {
  proxyUrl: "",
};
